# Security

Do not commit AP passwords, `/var/lib/bluetooth`, private keys, `.env` files or diagnostic output containing real device MAC addresses. Report security issues privately to the repository owner instead of opening a public issue with credentials or bonding data.
